import React from 'react';
import { LineChart, XAxis, YAxis, CartesianGrid, Line } from 'recharts';
const StatisticsTotal = ({value}) => {
    // console.log(value)
    return (
        <div>

        </div>
    );
};

export default StatisticsTotal;